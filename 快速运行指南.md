# 模型加载、评估和再训练 - 快速运行指南

## 前置要求
- Docker Desktop 已安装并运行
- NVIDIA GPU 驱动已安装（RTX 5080）
- Docker 已配置 GPU 支持

## 快速启动

### 方法1：使用批处理脚本（推荐）
双击运行 `run_docker.bat`，会自动：
1. 启动 Docker 容器
2. 安装依赖
3. 启动 Jupyter Lab
4. 在浏览器中打开

### 方法2：手动启动
```bash
docker-compose up -d
```

然后访问：http://localhost:8891

## 使用说明

1. **打开 Notebook**
   - 在 Jupyter Lab 中打开 `model_retrain_f1.ipynb`

2. **模型准备**
   - 如果 `models/distilbert-base-uncased-finetuned-squad/` 不存在
   - Notebook 会自动进行初始训练（需要约 1-2 小时）

3. **运行顺序**
   - 按顺序运行所有 cell
   - 评估和再训练过程需要一定时间

4. **训练时间估算**
   - 初始模型评估：约 10-15 分钟
   - 策略1（继续训练）：约 1-2 小时
   - 策略2（调整学习率）：约 1.5-2.5 小时
   - **总计：约 3-5 小时**（如果已有初始模型）

## 查看日志
```bash
docker-compose logs -f
```

## 停止容器
```bash
docker-compose down
```

## 注意事项

1. **初始模型**：
   - 如果没有预训练模型，需要先运行初始训练
   - 可以使用课程代码先训练一个基础模型

2. **GPU 内存**：
   - RTX 5080 有足够的显存
   - 当前配置已优化

3. **模型保存**：
   - 训练好的模型保存在 `./models/` 目录
   - 不同策略的模型保存在不同子目录

4. **F1 Score 提升**：
   - 不同策略可能提升不同
   - 建议尝试多种策略找到最佳配置

## 故障排除

### GPU 不可用
检查 Docker GPU 支持：
```bash
docker run --rm --gpus all nvidia/cuda:12.0.0-base-ubuntu22.04 nvidia-smi
```

### 端口被占用
修改 `docker-compose.yml` 中的端口号（8891）

### 模型加载失败
确保模型路径正确，或让 Notebook 自动进行初始训练

